# 项目优势 — svn-git文件版本管理

> 为什么值得用 / 为什么值得开源

## 1. 双引擎统一：SVN 与 Git 一套界面

市面上工具要么只支持 Git（SourceTree、GitKraken、Fork），要么 SVN 工具老旧难用（TortoiseSVN 仅 Windows）。本工具**同时深度支持两种版本库**：

- 自动识别仓库类型，同样的操作（状态/历史/差异/分支/标签/提交/更新/还原）在 SVN 和 Git 下体验一致
- 适合**国内企业环境**：大量团队仍用 SVN 服务器，同时个人项目用 Git——一个工具全覆盖
- 分支/标签等概念差异被抽象统一（SVN 的目录复制 vs Git 的指针，界面无感知）

## 2. 并发修改防护：行业内领先的完整闭环

普通工具只告诉你"提交被拒"或"有冲突"；本工具提供**三层防护闭环**：

```
预警 → 拦截 → 解决
```

- **行级冲突检测**：提交时精确到"哪个文件第几行冲突"（而非文件级），**强制拦截**不让你提交出错版本，并给出 备份→删除→更新→手动合并 的处理指引
- **服务器更新交集提示**：服务器有新提交时，仅当**待提交文件与服务器更新文件有交集**才提示（双击即可查看差异、可先更新），无交集不打扰
- **2 分钟主动监控**：你正在修改的文件被他人先提交 → 主动弹警示条，一键查看"对方改了哪里 / 你改了哪里"双 diff 对比
- **三方冲突解决器**：基础/本地/对方三栏 + 逐文件采用 + 手动编辑，冲突处理不再靠命令行

对比：TortoiseSVN 冲突时给你 .mine/.r 文件自己拼；本工具把整个过程图形化了。

## 3. 零部署负担：一个 AppImage 跑起来

- 单个 AppImage（约 96MB）内置 Node/Electron 运行时，**目标机器无需安装任何语言环境**
- 双击运行 → 自动打开浏览器界面
- 环境自检：svn/git 缺失时提示 + 一键安装引导（免密 sudo 自动装，否则给手动命令）

## 4. 轻量与安全

| 维度 | 说明 |
|---|---|
| 后端零框架 | 纯 Node 内置 http，无 Express/数据库，无重型依赖 |
| 本地服务 | 只监听 127.0.0.1 随机端口，不对外暴露 |
| 凭据安全 | 密码走 `--password-from-stdin`（ps 不可见），配置 600 权限 |
| 路径防护 | 全部接口防路径穿越（403） |
| 无注入 | 无 shell 拼接，参数全部数组传递 |

## 5. 现代界面体验（同类工具少有的用心）

- **三种文件视图**：列表 / 树 / 文件浏览器图标网格（自适应列数、彩色图标、状态角标、修改数量标识）
- **多选批量操作**：矩形框选 / Ctrl 点选 / Shift 范围选，右键一次处理整个选中集合（添加/提交/还原/删除/复制路径）
- **并排差异对比**：左原版右当前、修改行 M 标记、滚动条绿红点全局预览、点击修改处双向跳转、语法高亮；**分隔栏可拖拽调宽，左右栏独立搜索**
- **六套浅色主题**、字号调节、全键盘导航、右键上下文菜单（按状态动态显示可用操作）
- **弹窗体验**：全部弹窗可 8 方向拖拽调整大小、右上角最大化/还原，点击外部不会误关
- **全套彩色 SVG 图标**（不依赖系统 emoji 字体，任何 Linux 显示一致）
- 中文本地化，中文文件名/中文提交信息完整支持（实测修复过 E000022 编码问题）

## 6. 工程质量

- **约 8,900 行 TypeScript/TSX/CSS**（strict 模式 + noUncheckedIndexedAccess），模块化三层架构
- **61 项自动化断言**（VCS 层 32 + 扩展 29）+ 两轮 Agent 全功能回归（含真实冲突场景）
- 中英文输出兼容（系统 zh_CN.UTF-8 环境实测）
- 完整开发模式热更新（改代码即生效，免打包迭代）

## 7. 为什么开源有价值

- **填补空白**：Linux 下 SVN + Git 双引擎的现代图形工具几乎没有（TortoiseSVN 是 Windows 专属，Linux 下 SVN 只能命令行）
- **企业场景刚需**：国内大量 SVN 存量项目，需要图形化工具降低使用门槛
- **架构可扩展**：VCS 抽象层清晰，未来可加 Mercurial/Fossil 等新引擎
- **完整文档**：架构文档 + 截图 + 测试脚本，新手也能维护

## 一句话总结

> **Linux 上唯一同时把 SVN 和 Git 做到"现代图形化 + 并发防护闭环"的工具，零部署、轻量、安全、中文友好。**
